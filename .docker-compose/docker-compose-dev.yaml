version: "3.8"
services:
  common-backend:
    build:
      context: ../
      dockerfile: Dockerfile
    restart: always
    command: sleep infinity
    environment:
      - SERVICE=fantasy-baseball-common-backend
